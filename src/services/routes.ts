export const authRoutes = {
    login: "auth/login/",
    register: "auth/register/",
    refresh: "auth/refresh/",
    verify: "auth/verify",
};

export const kanbanRoutes = {
    task: "tasks/",
};
